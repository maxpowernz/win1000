export interface EducationDocument {
  documentName: string;
  uploadedBy: string;
  schoolId: number;
  dateUploaded: string;
}

export interface AgencyDocument {
  documentName: string;
  uploadedBy: string;
  agencyId: number;
  dateUploaded: string;
}
